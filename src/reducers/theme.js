const initialState = {
    textColor: '#232F34',
    cardColor: '#FFFFFF',
    viewColor: '#c7c7c7'
};

export default function state(state = initialState, action) {
    switch (action.type) {


        default:
            return state;
    }
}
